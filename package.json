{
  "name": "erp-portal-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm install --prefix server",
    "start": "npm start --prefix server",
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix server\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
